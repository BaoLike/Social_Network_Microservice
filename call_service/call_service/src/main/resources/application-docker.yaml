spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      uri: mongodb://root:root@mongo:27017/call-service?authSource=admin

app:
  service:
    identity: http://identity:8080/identity
    notification: http://notification:8082/notification
